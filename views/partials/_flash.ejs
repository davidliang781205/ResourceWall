<div class="alert alert-warning">
  <% if (errors.length) { %>
    <% errors.forEach((error) => { %>
      <p>Error: <%= error %></p>
    <% }) %>
  <% } %>
  <% if (info.length) { %>
    <% info.forEach((message) => { %>
      <p>Info: <%= message %></p>
    <% }) %>
  <% } %>
</div>
